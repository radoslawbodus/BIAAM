// Create random permutation of size "size"
int *random_permutation(int size);

// Wrapper for random_permutation function for time testing purposes
void random_permutation_time_test(int size);

// Print the array
void print_array(int *, int);

// Swap two values
void swap(int *, int *);

